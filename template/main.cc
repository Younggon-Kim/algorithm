#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    freopen(getenv("out"), "w", stdout);
    freopen(getenv("in"), "r", stdin);
}